# T001 Project Setup

**Status:** In Progress

## Current Scope
1. Create the requested folder structure for docs, UI primitives, sections, animations, and shared library code.
2. Document the hard operating rules for future implementation tasks in `docs/agent_guidelines.md`.
3. Establish a premium Tailwind theme with reusable design tokens and global styling defaults.
4. Add a reusable `Reveal` animation wrapper powered by Framer Motion.
5. Replace the starter homepage with a section-based landing page shell.
6. Validate the project setup and dependencies so the boilerplate is ready for the first design ticket.
